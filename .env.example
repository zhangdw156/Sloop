# LLM 配置
# OpenAI API Key (必需)
OPENAI_API_KEY=your_openai_api_key_here

# 模型名称 (可选，默认: gpt-4o-mini)
MODEL_NAME=gpt-4o-mini

# OpenAI API Base URL (可选，用于自定义端点如 vLLM、DeepSeek 等)
# OPENAI_API_BASE=https://api.openai.com/v1

# 温度参数 (可选，范围: 0.0-2.0，默认: 0.7)
# TEMPERATURE=0.7

# 最大token数 (可选，默认: 4096)
# MAX_TOKENS=4096

# 请求超时时间(秒) (可选，默认: 60)
# TIMEOUT=60

# 示例配置:
# OPENAI_API_KEY=sk-...
# MODEL_NAME=gpt-4o
# TEMPERATURE=0.8
# MAX_TOKENS=8192
# TIMEOUT=120
