# LLM 配置
# OpenAI API Key (必需)
OPENAI_API_KEY=your_openai_api_key_here

# 模型名称 (可选，默认: gpt-4o-mini)
MODEL_NAME=gpt-4o-mini

# OpenAI API Base URL (可选，用于自定义端点如 vLLM、DeepSeek 等)
# OPENAI_API_BASE=https://api.openai.com/v1

# 温度参数 (可选，范围: 0.0-2.0，默认: 0.7)
# TEMPERATURE=0.7

# 最大token数 (可选，默认: 4096)
# MAX_TOKENS=4096

# 请求超时时间(秒) (可选，默认: 60)
# TIMEOUT=60

# Embedding 配置 (用于 RAG 模式)
# Embedding 提供商 (可选，默认: openai)
# EMBEDDING_PROVIDER=openai

# Embedding 模型名称 (可选，默认: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small

# Embedding API Key (可选，默认复用 OPENAI_API_KEY)
# EMBEDDING_API_KEY=your_embedding_api_key_here

# Embedding API Base URL (可选，用于自定义端点)
# EMBEDDING_BASE_URL=https://api.openai.com/v1

# 示例配置:
# OPENAI_API_KEY=sk-...
# MODEL_NAME=gpt-4o
# TEMPERATURE=0.8
# MAX_TOKENS=8192
# TIMEOUT=120
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=sk-... (如果不设置，会复用 OPENAI_API_KEY)
